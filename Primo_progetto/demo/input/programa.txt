int sum (int a1, int b1){
    int c = a1 + b1; 
    return c;
}

int fact (int n, int x, int y, char z, double j, int k){
    int a;

    //a = a + 1;

    //x = a + b;

    a = sum(x, y);

    return a;
}

void main(){
    int ciao = 1, hola = 2, hello;

    ciao = sum(ciao, hola);
}